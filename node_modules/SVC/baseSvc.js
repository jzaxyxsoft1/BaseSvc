/**
 * Created by wdg on 14-2-28.
 */
Array.prototype.Sum = function (f) {
    var _r = 0;
    if (typeof f == 'string') {
        this.forEach(function (i) {
            _r += i[f];
        });
    }
    else {
        this.forEach(function (i) {
            _r += f(i);
        })
    }
    return r;
};

var BaseSvc = {
    convertOption: function (obj) {
        Object.keys(obj).forEach(function (pi) {
            obj[pi] = Number(obj[pi]);
        })
    },
    MediaType:{'img':'img','flash':'flash'}

};
exports.BaseSvc = BaseSvc;
